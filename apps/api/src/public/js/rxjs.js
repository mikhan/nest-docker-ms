/**
 * @typedef {import('rxjs')} rxjs
 * @typedef {import('rxjs/operators')} operators
 */

/** @type rxjs */
export const rxjs = window['rxjs']
/** @type operators */
export const operators = window['rxjs']['operators']
